# Tests

This folder describes the onboarding, install, and first-experiment checks that should run against ResearchLoop.

Suggested scenarios:

- blank folder
- real repo with history
- GitHub URL handoff
- local path handoff
- no repo fallback to demo
- first experiment suggestion after install
- dashboard with multiple loss curves
- history-first idea generation
