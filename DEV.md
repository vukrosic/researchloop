# Development Control Room

This repo contains a local-only control room under `researchloop-dev/`.

It is for:

- onboarding simulations
- local-build install tests
- first-experiment checks
- transcript and summary capture
- release/deployment helpers

It is not part of the npm package payload.

The npm package stays focused on the product surface that researchers need.
